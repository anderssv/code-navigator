# Code Navigator - Claude Instructions

See `AGENTS.md` for full project instructions.

When working on a project that has code-navigator installed, run `./gradlew cnavAgentHelp -Pllm=true` to get detailed instructions on available tasks, parameters, workflows, result interpretation, and JSON schemas.
